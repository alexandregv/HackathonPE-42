<%= form_with(model: competence, local: true) do |form| %>
  <% if competence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(competence.errors.count, "error") %> prohibited this competence from being saved:</h2>

      <ul>
      <% competence.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :code %>
    <%= form.text_field :code %>
  </div>

  <div class="field">
    <%= form.label :nom %>
    <%= form.text_field :nom %>
  </div>

  <div class="field">
    <%= form.label :membre_id %>
    <%= form.text_field :membre_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
